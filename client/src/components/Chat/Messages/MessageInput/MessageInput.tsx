import React from 'react';

import { Input } from 'antd';

const MessageInput = (): React.ReactElement => {
  return <Input size='large' placeholder='Enter your message and press Enter' />;
};

export default MessageInput;
