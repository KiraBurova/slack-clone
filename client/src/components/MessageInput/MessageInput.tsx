import React from 'react';

import { Input } from 'antd';

const MessageInput = () => {
  return (
    <Input size='large' placeholder='Enter your message and press Enter' />
  );
};

export default MessageInput;
